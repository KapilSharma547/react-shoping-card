/*
import react, { useReducer } from "react";

const intialstate = 0;

const reduce = (state, action) => {
    if (action.type === '+') {
        return state + 1;
    }
    if (action.type === '-') {
        return state - 1;
    }
    
    return state;
}

const Usereducer = () => {

    const [state, dispatch] = useReducer(reduce, intialstate);

    return (
        <>
            <p>{state}</p><br></br>
            <button onClick={() => { dispatch({ type: '+' }) }}>+</button><br></br><br />
            <button onClick={() => { dispatch({ type: '-' }) }}>-</button>
        </>
    );
}

export default Usereducer;
*/

// 
// 
// 
// 
// 
// 
// 
// 
// 
// 
// 
// map method


import react from "react";


const Kapils = () => {
    const Kapils = ['kapil', 'sharma', 'rohit', 'kumar'];
    console.log(Kapils);
    return (
        <>
        <h1>kapil</h1>
        const new = old.map()

        </>
    );


};

export default Kapils;





